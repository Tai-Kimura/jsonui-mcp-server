{
  "name": "Check",
  "description": "Check component",
  "aliases": [],
  "platforms": {
    "swift_generated": true,
    "swift_dynamic": true,
    "kotlin_generated": true,
    "kotlin_dynamic": true,
    "react": true
  },
  "attributes": {
    "checked": {
      "type": [
        "boolean",
        "binding"
      ],
      "description": "Check state (binding for two-way)"
    },
    "isOn": {
      "type": [
        "boolean",
        "binding"
      ],
      "description": "Checked state (binding for two-way)"
    },
    "bind": {
      "type": "binding",
      "description": "Two-way binding @{variable}"
    },
    "label": {
      "type": [
        "string",
        "binding"
      ],
      "description": "Check label text (can be data binding)"
    },
    "text": {
      "type": [
        "string",
        "binding"
      ],
      "description": "Check label text alias (can be data binding)"
    },
    "icon": {
      "type": "string",
      "description": "Icon name for unchecked state"
    },
    "src": {
      "type": "string",
      "description": "Image source for unchecked state"
    },
    "selectedIcon": {
      "type": "string",
      "description": "Selected icon name"
    },
    "onSrc": {
      "type": "string",
      "description": "Image source for checked state"
    },
    "enabled": {
      "type": [
        "boolean",
        "binding"
      ],
      "description": "Whether enabled (can be data binding)"
    },
    "font": {
      "type": [
        "string",
        "binding"
      ],
      "description": "Font name (binding supported)"
    },
    "fontSize": {
      "type": [
        "number",
        "binding"
      ],
      "description": "Font size (binding supported)"
    },
    "fontColor": {
      "type": [
        "string",
        "binding"
      ],
      "description": "Text color - hex string or color name from colors.json (binding supported)"
    },
    "spacing": {
      "type": [
        "number",
        "binding"
      ],
      "description": "Space between icon and text (binding supported)"
    },
    "onValueChange": {
      "type": "binding",
      "description": "Value change handler - binding only (@{functionName})"
    }
  },
  "bindingBehavior": {
    "checked": {
      "direction": "read-only"
    },
    "isOn": {
      "direction": "read-only"
    },
    "bind": {
      "direction": "read-only"
    },
    "label": {
      "direction": "read-only"
    },
    "text": {
      "direction": "read-only"
    },
    "enabled": {
      "direction": "read-only"
    },
    "font": {
      "direction": "read-only"
    },
    "fontSize": {
      "direction": "read-only"
    },
    "fontColor": {
      "direction": "read-only"
    },
    "spacing": {
      "direction": "read-only"
    },
    "onValueChange": {
      "direction": "read-only"
    }
  },
  "platformSpecific": {},
  "rules": []
}